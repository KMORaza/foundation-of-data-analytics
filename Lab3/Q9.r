# Create character vector "fruits"
fruits <- c("apple", "banana", "orange", "grape", "mango")
# Sort the vector in alphabetical order
sorted_fruits <- sort(fruits)
# Print the sorted vector
print(sorted_fruits)
